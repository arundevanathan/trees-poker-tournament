// For local development, replace __FIREBASE_API_KEY__ in firebase-config.js with your real API key.
// Do not commit the real key — firebase-config.js uses a placeholder that the CI workflow replaces at deploy time.
